{% for comment in comments %}
	{% if comment.mark is null %}
		{{ include('G2sAppBundle:Apps:show-one-comment-nolayout.html.twig', {'title': comment.title, 'comment': comment.comment, 'platformName': comment.appInfo.platform.name, 'mark': null }) }}
	{% else %}
		{{ include('G2sAppBundle:Apps:show-one-comment-nolayout.html.twig', {'title': comment.title, 'comment': comment.comment, 'platformName': comment.appInfo.platform.name, 'mark': comment.mark.mark }) }}
	{% endif %}
{% else %}
	<div id="no-comment" class="alert alert-info">
		<h1>
			<span class="glyphicon glyphicon-arrow-up"/>
			Il n'y a aucun commentaire, postez le votre !
			<span class="glyphicon glyphicon-arrow-up"/>
		</h1>
	<div>
{% endfor %}
